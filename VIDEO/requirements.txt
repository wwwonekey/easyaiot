--index-url https://pypi.tuna.tsinghua.edu.cn/simple
# 本地 / 服务器直装（非 Docker）
#   pip install -r requirements.txt
# CUDA 版 PyTorch 请按官方说明选择：https://pytorch.org/get-started/locally/
#
# Docker 构建请使用 requirements-docker.txt（见 Dockerfile），勿与本文件混用。

-r requirements-base.txt

# PyTorch（与 Docker 基础镜像 pytorch/pytorch:2.9.0-cuda12.8-cudnn9-devel 对齐）
torch>=2.9.0
torchvision>=0.24.0
