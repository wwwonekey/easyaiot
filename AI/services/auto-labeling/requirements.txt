--index-url https://pypi.tuna.tsinghua.edu.cn/simple
# 本地 / 服务器直装（非 Docker）
#   pip install -r requirements.txt
# Docker 构建请使用 requirements-docker.txt（见 Dockerfile）

-r requirements-base.txt

# 与标注平台 Docker 基础镜像 pytorch/pytorch:2.9.0-cuda12.8-cudnn9-runtime 对齐（torch 2.9.x）
torch>=2.9.0
torchvision>=0.24.0
